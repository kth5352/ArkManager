# Ark Manager — 기술 아키텍처 문서

개인 게임/미디어 라이브러리를 관리하는 Windows용 Electron 데스크톱 애플리케이션의 설계와 구현을 정리한 문서입니다. 폴더 단위로 등록한 라이브러리를 스캔해 식별 코드를 인식하고, 여러 외부 소스에서 메타데이터(표지, 제목, 제작사, 발매일, 태그)를 가져와 붙여주며, 파일 탐색기·미디어 플레이어·세이브 데이터 관리 기능을 하나의 앱 안에 통합합니다.

**규모**: 약 2.5주 개발 기간, 468개 커밋, 렌더러·메인 프로세스 합산 약 21,000줄의 TypeScript(테스트 제외), 렌더러 28개·메인 프로세스 55개 이상의 유닛 테스트 파일.

**기술 스택**: Electron + electron-vite · React 19 + TypeScript(strict) · Tailwind CSS v4 + shadcn/ui(Radix) · Zustand + TanStack Query v5 + TanStack Router · Drizzle ORM + better-sqlite3 · dnd-kit · react-window(가상 스크롤) · Vitest

---

## 1. 전체 아키텍처

Electron의 3계층 구조(메인 프로세스 / 프리로드 / 렌더러)를 엄격하게 지킵니다.

```
electron/main/      메인 프로세스 — Node 권한 전체. IPC 핸들러, SQLite, 파일 스캐너,
                    메타데이터 크롤러, 파일 시스템 작업, 커스텀 프로토콜(thumb://, media://)
electron/preload/    렌더러에 노출할 API 표면을 정의하는 유일한 진입점 (contextBridge)
src/                 렌더러 프로세스 — React 19, Node API 직접 접근 불가
shared/              메인·렌더러 양쪽에서 import하는 타입/Zod 스키마/순수 로직
```

렌더러는 `window.api.*`로 노출된 타입 지정 함수만 호출할 수 있고, 그 밖의 Node.js API에는 접근할 수 없습니다. `contextBridge.exposeInMainWorld('api', api)` 단 한 줄이 유일한 브리지이며, `export type Api = typeof api`로 렌더러 쪽에 전체 타입을 그대로 전달해 두 프로세스 간 타입이 어긋나지 않도록 합니다.

### IPC 설계 원칙

`shared/types/ipc.ts` 하나에 77개의 채널 상수(`IPC_CHANNELS`)와 요청/응답 스키마를 전부 모아둡니다. 거의 모든 핸들러가 동일한 패턴을 따릅니다.

```ts
ipcMain.handle(IPC_CHANNELS.X, (_event, payload: unknown) => {
  const { ... } = XRequestSchema.parse(payload)
  // ...
})
```

payload는 항상 `unknown`으로 받고 Zod 스키마로 `.parse()`한 뒤에만 사용합니다. 프리로드가 제공하는 타입 지정 래퍼 함수를 우회해 렌더러가 임의의 payload를 직접 보내더라도(버그든, 손상된 상태든) 메인 프로세스 경계에서 걸러지도록 하기 위한 장치입니다. 진행 상황을 알리는 push 전용 채널(`SCANNER_SCAN_PROGRESS`, `METADATA_BULK_CRAWL_PROGRESS`, `UPDATE_STATUS` 등)은 `ipcMain.handle`이 아니라 `webContents.send`로 별도 관리합니다.

### 신뢰 경계

렌더러가 호출할 수 있는 모든 파일 시스템 작업(이름변경/이동/삭제, 썸네일 서빙, 실행 파일 지정, 가사 파일 조회 등)은 대상 경로가 **등록된 라이브러리 경로 아래**에 있는지를 메인 프로세스에서 검증한 뒤에만 수행됩니다. 핵심 함수는 다음과 같습니다.

```ts
export function isPathWithinAnyLibrary(entryPath: string, libraryPaths: string[]): boolean {
  const normalizedEntry = normalizeForComparison(resolve(entryPath))
  return libraryPaths.some((libraryPath) => {
    const normalizedLibrary = normalizeForComparison(resolve(libraryPath))
    return (
      normalizedEntry === normalizedLibrary || normalizedEntry.startsWith(`${normalizedLibrary}/`)
    )
  })
}
```

단순 문자열 접두사 비교가 아니라 `path.resolve()`를 먼저 적용해 `..`/`.` 세그먼트를 정리한 뒤 비교합니다. 그렇지 않으면 `C:/Games/LibraryA/../../Windows/system.ini` 같은 경로가 문자열상으로는 라이브러리 접두사와 일치해버리는 경로 탈출 문제가 생기기 때문입니다. Windows 경로는 대소문자와 구분자(`\` vs `/`)가 혼용되므로 비교 전에 소문자화 + 슬래시 통일도 함께 수행합니다.

이 검증은 이름변경/이동/삭제(`explorerHandlers.ts`), 썸네일 프로토콜(`thumb://`), 가사 파일 조회, 미디어 썸네일 덮어쓰기 등 파일 시스템에 닿는 모든 핸들러에서 반복적으로 사용되며, 위반 시 결과는 항상 "조용히 거부"입니다 — 예외를 던지거나 실패로 표시할 뿐, 다른 경로로 리다이렉트하지 않습니다. 다만 이동(move)의 **목적지**는 의도적으로 이 검사에서 제외했는데, 네이티브 폴더 선택 대화상자를 통한 이동은 백업 드라이브 등 라이브러리 밖으로 옮기는 것을 정당한 사용으로 보기 때문입니다. 대신 렌더러 쪽 드래그 앤 드롭 이동에서는 (한 번의 실수로 되돌리기 어려운 위치로 파일이 사라지는 것을 막기 위해) 별도로 라이브러리 경계를 하드 체크합니다 — 같은 문제를 계층마다 다른 리스크 수준에 맞춰 다르게 처리한 예입니다.

---

## 2. 데이터 계층

better-sqlite3 위에 Drizzle ORM을 얹어 타입 지정 쿼리를 작성하지만, **런타임 마이그레이션 파이프라인은 사용하지 않습니다.** 스키마는 `electron/main/database/client.ts`에서 `CREATE TABLE IF NOT EXISTS` DDL을 실행한 뒤, `ensureColumns()` 헬퍼가 `PRAGMA table_info(table)`로 실제 컬럼을 읽어 DDL과 비교하고 없는 컬럼만 `ALTER TABLE ... ADD COLUMN`으로 채워 넣는 방식입니다. `CREATE TABLE IF NOT EXISTS`는 이미 존재하는 테이블에는 아무 효과가 없기 때문에, 이후 기능 추가로 컬럼이 늘어난 테이블(`game_user_data`는 즐겨찾기 → 평점 → 실행 설정 → 플레이타임 → 세이브 경로 순으로 다섯 번 이상 확장됨)을 안전하게 마이그레이션하기 위한 장치입니다. `drizzle-kit`은 스키마 정의 문서화 용도로만 devDependency에 남아 있고, 실제 부팅 시퀀스에는 관여하지 않습니다.

`journal_mode = WAL`과 `busy_timeout = 5000`을 설정해 여러 IPC 핸들러가 같은 틱에 쓰기를 시도해도 `SQLITE_BUSY`가 나지 않도록 했습니다.

### 주요 테이블

| 테이블                      | 역할                                                                                               |
| --------------------------- | -------------------------------------------------------------------------------------------------- |
| `app_settings`              | 키/값 설정 저장소. 테마, 사이드바 폭, 언어, 미디어 폴더, 외부 메타데이터 API 설정, 창 닫기 동작 등 |
| `libraries`                 | 등록된 라이브러리 폴더                                                                             |
| `explorer_tabs`             | Explorer 탭 상태(경로, 순서, 활성 여부, 보기 방식) 영속화                                          |
| `game_metadata`             | 크롤링한 메타데이터 캐시 (제목/제작사/발매일/장르/표지 경로)                                       |
| `metadata_failures`         | 조회 실패 사유 기록 — 재시도 없이 이유를 보여주기 위함                                             |
| `game_user_data`            | 즐겨찾기, 평점, 메모, 실행 설정, 누적 플레이타임, 세이브 경로, 커스텀 표지                         |
| `path_code_overrides`       | 파일명에서 코드가 인식되지 않는 항목의 수동 코드 연동                                              |
| `save_snapshot_labels`      | 세이브 스냅샷별 메모/버전 라벨                                                                     |
| `media_thumbnail_overrides` | 표지를 파일에 직접 삽입할 수 없는 미디어의 앱 자체 썸네일                                          |
| `excluded_entries`          | 목록에서 개별적으로 숨긴 경로                                                                      |

**항목 식별 모델**: 각 항목은 파일명에서 인식된 **코드**(RJ/VJ/ST/VNV/VNR/GC) 또는 코드가 없을 경우 **정규화된 경로**로 식별됩니다. `resolveGameEntryKey()`가 이 우선순위를 한 곳에서 관리해 여러 핸들러가 각자 다른 규칙을 쓰지 않도록 합니다. 단, 숨김 목록(`excluded_entries`)만은 의도적으로 항상 경로 기준입니다 — 같은 코드를 가진 중복 사본 중 하나만 숨기고 싶은 경우가 실제로 있기 때문입니다.

---

## 3. 폴더 스캐너와 식별 코드 인식

### 코드 인식 정규식

```ts
const CODE_PATTERN = /(?<![A-Za-z0-9])((?:RJ|VJ|ST|VNV|VNR|GC)\d+)(?![0-9])/i
```

일반적인 `\b` 단어 경계 대신 부정 전방/후방탐색을 쓴 이유가 있습니다. 파일명 관례상 코드와 제목이 밑줄로 이어지는 경우(`RJ01234567_작품명`)가 많은데, `_`는 정규식에서 단어 문자로 취급되어 앞뒤에 경계가 생기지 않으므로 `\b(RJ|VJ|ST)\d+\b` 형태로는 이런 파일명에서 매치가 실패합니다. 대신 코드 앞에 영문자/숫자가 바로 붙어 있지 않고(`COST1234` 같은 오탐 방지), 뒤에 숫자가 더 이어지지 않는다는(자릿수 초과 매칭 방지) 조건만 직접 검사해, 밑줄·공백·한글·문자열 끝은 모두 허용하면서 실제 오탐만 걸러냅니다.

### VNDB 코드 분리 — 실제로 있었던 버그와 수정

VNDB는 "비주얼 노벨"(`v` 접두사)과 "릴리스"(`r` 접두사)를 서로 다른 개체로 관리하며, 두 ID 공간은 독립적이라 우연히 같은 숫자를 가질 수 있습니다(`v45775`와 `r45775`가 전혀 다른 작품을 가리킬 수 있음). 초기에는 `VN`/`VR`이라는 2글자 접두사로 구분했는데, 이 체계에서 실제로 코드가 뒤섞여 잘못된 작품 정보를 가져오는 문제가 발생했습니다. 이를 `VNV`(비주얼 노벨)/`VNR`(릴리스) 3글자 체계로 교체해 정규식·DB 키·URL 어디서든 모호함이 없도록 했고, 기존 데이터를 위한 1회성 마이그레이션을 작성했습니다.

```ts
export function legacyVndbCodeToCanonical(value: string): string | null {
  const vn = /^VN(\d+)$/.exec(value)
  if (vn) return `VNV${vn[1]}`
  const vr = /^VR(\d+)$/.exec(value)
  return vr ? `VNR${vr[1]}` : null
}
```

이 마이그레이션은 DB를 열 때마다 한 트랜잭션 안에서 4개 테이블(캐시, 실패 기록, 사용자 데이터, 세이브 라벨)에 걸쳐 실행되며, 더 이상 어디서도 참조되지 않는 낡은 캐시 항목은 이전하지 않고 정리합니다. 세이브 백업 폴더명도 같은 체계로 별도 마이그레이션합니다.

### 재귀 스캔의 폴더 접기 휴리스틱

라이브러리 전체를 훑는 재귀 스캔에는 "이 폴더를 하나의 항목으로 볼 것인가, 안으로 들어가서 펼칠 것인가"를 판단하는 로직이 있습니다. 코드가 있는 파일/폴더는 항상 리프(leaf)로 취급합니다. 코드가 없는 폴더는 (1) 직속 파일이 하나 이상 있고, (2) 그 안에 코드가 붙은 자식이 정확히 하나만 있는 경우가 아니며(그런 자식이 있다면 그 자식 스스로가 독립된 작품이라는 신호이므로 펼쳐야 함), (3) 직속 압축파일이 2개 이상은 아닌 경우(여러 압축파일을 직접 담고 있으면 시리즈 폴더 같은 다중 작품 컨테이너로 간주) — 이 세 조건을 모두 만족할 때만 하나의 항목으로 접습니다. 표지/CG 이미지는 이 판단에서 아예 제외되며(파일명 관례가 없어 개별 작품 구분에 쓸 수 없음), 심볼릭 링크/정션은 상위 폴더를 가리키는 경우의 무한 루프를 막기 위해 재귀하지 않습니다.

---

## 4. 메타데이터 수집 파이프라인

코드의 종류(`code.type`)에 따라 완전히 다른 조회 전략을 씁니다.

- **ST(Steam)**: Steam 단일 소스
- **VNV/VNR(VNDB)**: VNDB 공식 Kana API 단일 소스 (스크래핑이 아닌 정식 API)
- **GC(Getchu)**: Getchu 단일 소스
- **RJ/VJ**: **DLsite HTML 페이지 → DLsite JSON 폴백 → 사용자 설정 외부 API** 순서로 시도하는 다단계 폴백 체인. 각 단계는 순서대로 시도되고, 실패 사유(`not_found`/`blocked`/`network`/`parse`/`provider_error`)를 `metadata_failures` 테이블에 남겨 "왜 정보가 없는지"를 UI에서 설명할 수 있게 합니다.

### 소스별로 다른 기술적 처리

- **DLsite**: 실제 작품 페이지를 cheerio로 파싱. 1차 실패 시 별도 내부 JSON 엔드포인트로 재시도.
- **Steam**: 연령 확인 페이지를 실제로 거치지 않고, Steam이 그 확인 절차에서 자체적으로 설정하는 쿠키를 요청 시점에 미리 실어 보내 상점 페이지를 바로 가져옵니다.
- **Getchu**: 응답을 `arrayBuffer()`로 받아 `TextDecoder('euc-jp')`로 직접 디코딩합니다 — 기본 `response.text()`는 UTF-8을 가정해 일본어 텍스트를 깨뜨리기 때문에, 실제 응답의 인코딩을 확인해 맞춘 처리입니다. 상대 경로 이미지 URL 보정, 전각 콜론(`：`) 접미사가 붙은 스펙 표 라벨, `YYYY/MM/DD` 날짜 형식 등 사이트 고유의 마크업 특성도 별도로 처리합니다.
- **표지 이미지 다운로드 공통 처리**: 일부 호스트가 리퍼러 없는 이미지 요청을 403으로 차단하는 것을 확인해, 실제 브라우저가 그 작품 페이지에서 이미지를 보여줄 때 보내는 것과 동일한 same-origin `Referer` 헤더를 함께 보냅니다. 다운로드한 이미지는 `sharp`로 webp로 변환해 캐시합니다.
- **외부 메타데이터 API**: 설정에서 URL/사용 여부/Bearer 토큰을 구성할 수 있는 커스텀 프로바이더. HTTPS가 아니면(로컬 개발용 loopback 주소 제외) 거부하고, 응답은 Zod 스키마로 검증한 뒤에만 신뢰합니다.

### 백그라운드 일괄 크롤 큐

캐시에 없는 코드들을 한꺼번에 조회할 때, 병렬이 아니라 **1초 간격으로 하나씩 순차 처리**하도록 명시적으로 설계했습니다. DLsite에 문서화된 요청 제한은 없지만, 동시에 여러 요청을 몰아서 보내면 이 앱의 IP가 차단될 위험이 있다고 판단한 결정입니다. 세션 내에서 이미 시도한 코드는 `Set`으로 추적해 여러 페이지에서 중복 요청되지 않도록 하고, 개별 코드 실패는 전체 큐를 막지 않고 건너뜁니다.

---

## 5. 상태 관리: Zustand와 TanStack Query의 역할 분담

규칙은 명확합니다 — **앱 재시작 후에도 남아야 하거나 OS/파일시스템/네트워크에서 온 데이터는 TanStack Query, "지금 사용자가 뭘 하고 있는가"에 해당하는 휘발성 상태는 Zustand.**

TanStack Query가 감싸는 것: 폴더 스캔(경로별 쿼리 키, 5분 `staleTime`으로 포커스 복귀 시마다 재스캔되는 것을 방지), 메타데이터, 설정값(사이드바 폭, 언어, 미디어 폴더 등 — 영속되는 설정은 전부 스토어가 아니라 쿼리+뮤테이션 쌍), 세이브 스냅샷, 파일 작업(성공 시 관련 쿼리를 무효화하는 뮤테이션), 썸네일/가사.

Zustand가 감싸는 것: Explorer 탭과 활성 탭 ID, 다중 선택 상태(여러 페이지에서 공유), 미디어 플레이어의 재생목록/재생 위치/반복·셔플 모드, 마지막 이동 기록(실행 취소용), 페이지 간 1회성 신호, 라이브러리 표시 필터.

유일한 예외는 Explorer 탭인데, Zustand 스토어 자체는 순수 인메모리로 두고 그 위에 별도의 500ms 디바운스 저장 훅을 얹어 SQLite와 동기화합니다. 즉 영속화가 필요한 경우에도 "Zustand를 영속 계층으로 만드는" 대신 "평범한 Zustand 스토어 + 얇은 동기화 훅"을 택해, 스토어 자체의 단순함을 유지했습니다.

---

## 6. Explorer — 파일 탐색기 서브시스템

가장 복잡한 화면입니다. `ExplorerPage`가 페이지 전체에서 단 하나의 dnd-kit `DndContext`를 소유하고, 그 안에 폴더 트리 사이드바·탭 바·탭별 콘텐츠(목록/그리드)가 함께 들어갑니다.

### 드래그 앤 드롭 설계에서 내린 판단들

- **충돌 감지 전략**: `closestCenter`/`closestCorners` 대신 `pointerWithin`을 명시적으로 선택했습니다. 파일을 실제로 옮기는(되돌리기 까다로운) 동작이라, 포인터가 정말로 드롭 대상 위에 있을 때만 반응해야 한다고 판단했기 때문입니다.
- **같은 id를 draggable/droppable에 재사용**: 모든 행이 동시에 드래그 소스이자(폴더인 경우) 드롭 대상이며, 같은 `entry.path`를 id로 공유합니다. dnd-kit이 두 종류를 별도 레지스트리로 관리하기 때문에 문제가 없고, 오히려 "폴더를 자기 자신에게 드롭" 케이스가 기존의 `active.id === over.id` 가드에서 별도 코드 없이 자연스럽게 걸러집니다.
- **사이드바 드롭 대상의 id 충돌**: 폴더 트리 사이드바가 펼쳐졌을 때 그 노드가 가리키는 경로는 메인 화면의 행/브레드크럼과 동일한 경로를 가질 수 있습니다. dnd-kit은 id별로 하나의 전역 등록만 유지하므로, 이름 충돌을 막기 위해 사이드바 쪽 드롭 id에만 `tree:` 접두사를 붙이고(드롭 payload의 실제 경로는 그대로 유지) 드롭 처리 로직은 전혀 건드리지 않았습니다.
- **백엔드는 이동 목적지를 제한하지 않지만, 드래그 앤 드롭만 라이브러리 경계를 하드 체크**합니다. 네이티브 폴더 선택 대화상자를 통한 이동은 의도적으로 라이브러리 밖도 허용하는 다단계 흐름이지만, 실수로 누르기 쉬운 드래그 앤 드롭 한 번으로 되돌리기 어려운 위치까지 파일이 사라지는 일은 막아야 한다고 봤습니다.
- **폴더 트리의 자동 펼침·너비 동기화**는 `useEffect`가 아니라 렌더링 중 상태를 비교해 필요하면 즉시 `setState`하는 패턴을 씁니다. 이렇게 하면 활성 탭 변경이나 저장된 값 로딩 직후에도 한 프레임 늦게 반영되는 시각적 지연이 생기지 않습니다.

### 실행 취소 — 데이터 구조 하나로 "재실행"까지 얻은 설계

이동 실행 취소(Ctrl+Z)를 위한 전용 IPC는 없습니다. 실행 취소는 그냥 **기존 이동 뮤테이션을 반대 방향으로 다시 호출하는 것**입니다. 마지막 이동 기록은 하나의 슬롯만 갖는 스토어(`lastMoveStore`)에 저장되며, 새 이동이 기록될 때마다 — 그 "새 이동"이 실행 취소 자체이더라도 — 무조건 덮어씁니다. 이 단순한 규칙 덕분에 Ctrl+Z를 두 번 누르면 자연스럽게 "실행 취소를 취소"(=재실행)하게 되는데, 재실행을 위한 별도 분기를 전혀 작성하지 않고도 얻어진 동작입니다. 배치 이동이 여러 원본 폴더에 걸쳐 있을 수 있으므로(재귀 검색 결과에서 다중 선택한 경우), 실행 취소 시 원래 폴더별로 그룹을 나눠 각각 되돌립니다.

### 그 외

가상 스크롤(react-window)과 드래그 앤 드롭이 자연스럽게 맞물리는 이유는, 목록/그리드 두 형태 모두 같은 드래그 훅을 항목 단위로 사용하기 때문입니다 — 즉 등록이 DOM 위치가 아니라 항목 자체에 묶여 있어 스크롤 중 행이 재활용되어도 드래그 상태가 어긋나지 않습니다. 롱프레스(길게 눌러 다중 선택 모드 진입)는 언마운트 시 타이머를 확실히 정리하는데, 이는 react-window가 스크롤 중 행을 재활용하기 때문에 정리하지 않으면 몇 초 뒤 타이머가 발동했을 때 전혀 다른 항목이 선택되는 버그가 생길 수 있기 때문입니다.

---

## 7. 미디어 플레이어

### 두 창 사이의 상태 동기화

메인 창과 별도로 분리된 플레이어 창(각각 독립된 Electron `BrowserWindow`, 독립된 렌더러 프로세스)이 있는데, Electron 렌더러 프로세스끼리는 메모리를 공유할 방법이 없습니다. 이를 메인 프로세스를 경유하는 브로드캐스트로 해결했습니다 — 로컬에서 재생 상태가 바뀔 때마다 전체 상태를 메인 프로세스로 보내고, 메인 프로세스는 이를 다른 창에 다시 전달합니다. 수신한 상태를 그대로 반영하면 그 반영 자체가 다시 전송을 트리거하는 무한 루프가 생기므로, "지금 원격 상태를 반영하는 중"이라는 플래그로 이를 막습니다.

### 셔플 순서 유지

셔플은 매번 새로 뽑는 것이 아니라 켜는 시점에 한 번 순서를 생성하고(Fisher-Yates), 그 순서 안에서 위치 포인터만 앞뒤로 이동합니다. 이렇게 하면 뒤로 갔다가 다시 앞으로 가도 이미 정해진 순서를 그대로 재생하지, 매번 새로운 무작위 결과가 나오지 않습니다. 재생목록이 바뀌면(항목 추가/삭제) 셔플 순서 길이가 재생목록과 어긋날 수 있는데, 이를 감지해 그 자리에서 순서를 다시 생성합니다.

### 가사(LRC) 동기화

`[mm:ss.fff]` 타임스탬프를 파싱하는 순수 함수로 구현했고, 타임스탬프가 하나도 없는 파일은 정적 가사(스크롤만 되는 텍스트)로 자동 전환합니다. 현재 재생 위치에 해당하는 줄을 찾는 것은 단순 선형 탐색이며, 켜기/끄기 상태는 곡별로 따로 기억해 한 곡에서 꺼도 다음 곡에 영향이 없습니다.

### 파일 목록 새로고침이 재생을 끊지 않는 이유

폴더 새로고침은 관련 쿼리 캐시만 무효화할 뿐, 미디어 플레이어의 재생 상태는 독립된 Zustand 스토어(파일 경로/이름만 들고 있고 쿼리 캐시에서 파생되지 않음)에 있기 때문에 전혀 영향을 받지 않습니다. 이는 "화면을 완전히 새로고침하지 않고 필요한 부분만 갱신한다"는 설계가 부수적으로 만들어낸 이점입니다.

---

## 8. 세이브 데이터 관리

스냅샷 저장/복원 모두 동일한 diff 계산을 재사용하되, **같은 결과를 방향에 따라 다르게 라벨링**합니다 — 스냅샷에만 있는 파일은 "저장" 관점에서는 삭제될 파일이지만 "복원" 관점에서는 새로 생길 파일입니다. 실행 파일의 버전 정보(PE 버전 리소스)와 파일명 패턴을 함께 사용해 게임 버전을 자동으로 추정하고, 스냅샷 시점 버전과 현재 버전이 다르면 복원 전에 명시적인 2단계 확인을 요구합니다. 전체 삭제도 별도의 2단계 확인을 거칩니다.

---

## 9. 오디오 표지 삽입 — 실패해도 안전한 파일 쓰기

MP3/FLAC/M4A에 표지 이미지를 삽입할 때 원본 파일을 직접 건드리지 않도록 다음 순서로 처리합니다.

1. 원본을 고유한 이름으로 백업 (이미 존재하면 실패하도록 exclusive 플래그 사용)
2. 표지를 삽입한 결과를 **임시 파일**에 먼저 생성 (원본과 다른 경로)
3. 임시 파일이 실제로 재생 가능한지, 삽입된 표지가 디코딩 가능한지 검증
4. 검증 통과 시에만 임시 파일로 원본을 교체
5. 교체 직후 다시 한 번(이번엔 실제 파일 vs 백업을 비교하며) 독립적으로 검증
6. 두 번째 검증까지 통과해야 백업 삭제

어느 단계에서든 실패하면 백업으로 복원을 시도하고, 복원이 성공하면 복원된 파일과 백업의 해시(SHA-256)를 비교해 **완전히 일치할 때만** 백업을 지웁니다. 해시가 다르면(혹은 비교 자체가 실패하면) 백업을 지우지 않고 남겨둡니다 — 확실하지 않은 상태에서 유일한 복구 수단을 지우지 않기 위한 결정입니다. 표지를 파일에 직접 넣을 수 없는 형식(WAV 등)은 처음부터 이 과정을 시도하지 않고, 앱 자체적으로 관리하는 대체 썸네일을 사용합니다.

---

## 10. 국제화 — 컴파일 타임에 강제되는 번역 완전성

한국어 객체를 원본으로 두고, 일본어/영어 객체를 `Record<keyof typeof ko, string>` 타입으로 선언합니다.

```ts
const ko = { 'nav.gallery': '갤러리' /* ... */ }
const ja: Record<keyof typeof ko, string> = {/* ko의 모든 키가 없으면 컴파일 에러 */}
const en: Record<keyof typeof ko, string> = {/* 동일 */}
```

TypeScript의 구조적 타이핑 덕분에 한국어 쪽에 새 문자열 키를 추가하면 일본어/영어 객체에 대응 키가 없는 순간 **빌드가 실패**합니다. 런타임에 번역 누락을 발견하는 대신, 애초에 누락된 채로 배포될 수 없도록 만든 설계입니다.

---

## 11. 테스트 전략

렌더러 쪽에는 컴포넌트 렌더링 테스트가 없습니다. 대신 로직을 최대한 작은 순수 함수로 뽑아내고(이름변경 템플릿 엔진, 경로 정규화, LRC 파서, diff 라벨링, 셔플 알고리즘 등) 그 함수들을 촘촘히 테스트합니다. 컴포넌트 자체의 동작은 TypeScript strict 모드와 수동 검증에 맡기되, 까다로운 버그를 고친 자리에는 예외 없이 "왜 이렇게 처리했는가"를 설명하는 주석을 남겨 — 회귀 테스트는 없지만 회귀를 막기 위한 맥락은 코드에 그대로 남도록 했습니다. 메인 프로세스는 반대로 저장소·파일 작업·스캐너·크롤러·IPC 핸들러 전반에 걸쳐 훨씬 두껍게 테스트되어 있습니다 — 파일 시스템·DB·외부 네트워크에 닿는 코드일수록 자동화된 검증의 가치가 크다고 판단한 결과입니다.

---

## 12. 설계 전반에서 반복되는 원칙

- **모든 IPC 입력은 경계에서 검증한다** (Zod `.parse`), **모든 파일 시스템 접근은 경계에서 권한을 확인한다** (`isPathWithinAnyLibrary`) — 신뢰할 수 없는 입력이 시스템 깊숙이 들어가기 전에 걸러내는 일관된 원칙입니다.
- **실패를 조용히 감추지 않는다.** 메타데이터 조회 실패는 사유와 함께 기록되고, 파일 쓰기 실패는 백업 복원과 해시 검증까지 거칩니다.
- **비동기 상태 갱신의 "한 프레임 지연"을 신경 쓴다.** 렌더링 중 상태 동기화 패턴이 여러 곳에서 반복적으로 쓰이며, 서로 주석으로 근거를 공유합니다.
- **왜 그렇게 하지 않았는지를 기록한다.** 코드 곳곳에 "이 방법도 시도해봤지만 이런 이유로 버렸다"는 주석이 매우 촘촘합니다. 최종 선택뿐 아니라 검토했던 대안까지 기록해, 나중에 같은 트레이드오프를 다시 검토할 필요가 없도록 했습니다.
